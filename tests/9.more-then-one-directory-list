
set -e

c="lf / /dev /var /usr"

if [ ! "$($c)" ]; then
	echo "FAIL: unexpected failure while running: '${c}'"
	exit 1
fi


g=`lf -1 /dev | wc -l`

e=`ls -1 /dev | wc -l`

if [ "$g" != "$e" ]; then
	echo "FAIL: expecting: $e, got $g"
	exit 1
fi

